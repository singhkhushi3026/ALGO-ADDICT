bool twoArrEqual(int arr1[], int arr2[]) 
{ 
 
    if (arr1.length != arr2.length) {
        return false;
     }
    sort(arr1);
    sort(arr2);
    for (int i = 0 to i < arr1.length) 
        if (arr1[i] != arr2[i]) 
            return false;
            
   
    return true;
}
